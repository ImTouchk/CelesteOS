#ifndef TYPES_HPP
#define TYPES_HPP

#include <stdint.h>

using byte = unsigned char;
using u32  = unsigned int;
using u64  = unsigned long long;
using i32  = int;
using i64  = long long;
using f32  = float;
using f64  = double;

/* native number types */
using isize = i64;
using usize = u64;
using fsize = f64;


#endif // TYPES_HPP
