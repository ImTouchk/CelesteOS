#ifndef TYPES_HPP
#define TYPES_HPP

using byte = unsigned char;
using u32  = unsigned int;
using u64  = unsigned long;
using i32  = int;
using i64  = long long;
using f32  = float;
using f64  = double;

#endif // TYPES_HPP
